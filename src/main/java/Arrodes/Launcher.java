package arrodes;

import javafx.application.Application;

/**
 * A Launcher class to workaround classpath issues and start the program
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Arrodes.class, args);
    }
}
